---
'midi-ports': patch
---

Docs: highlight the 3.2.0 features (cross-platform names, persistence, roles, `waitFor`, and the `midi-ports/testing` mock) in the README install section. No code changes.
